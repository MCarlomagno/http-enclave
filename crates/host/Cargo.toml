[package]
name = "host"
version = "0.1.0"
edition = "2021"


[dependencies]
anyhow = "1"
tokio = { version = "1", features = ["full"] }
# vsock is optional; TCP fallback supported
tokio-vsock = { version = "0.7", optional = true }

[features]
default = ["tcp"]
tcp = []
vsock = ["dep:tokio-vsock"]